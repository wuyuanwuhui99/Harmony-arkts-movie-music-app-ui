import * as colors from './color';
import * as size from './size';


@Styles function tabIconSize  () {
    .width(30)
    .height(30)
    .margin({bottom:size.smallPadding})
}

export default tabIconSize
